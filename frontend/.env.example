# Copy to .env.local for local overrides (Vite reads VITE_* vars). Never commit real secrets.

# Base URL of the WeatherGPT backend API. Defaults to the local dev server if unset.
# Set this to the deployed backend URL in production.
VITE_API_BASE_URL=http://localhost:8000
